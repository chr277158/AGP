---
# ====== Page: Page globale ==================================
id: 0
identification: 
  name: Page globale

page-items: 
- # ====== Page Item: P0_ROLE ==================================
  id: 7557156991892851
  identification: 
    name: P0_ROLE
    type: Masqué

  settings: 
    valeur-protégée: true

  layout: 
    sequence: 3
    region: No Parent
    slot: BODY

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: SQL Query (return single value)
    sql-query: |
      SELECT role_static_id
      FROM apex_appl_acl_user_roles
      WHERE application_id = :APP_ID
        AND user_name = :APP_USER;
    used: Only when current value in session state is null

  session-state: 
    data-type: VARCHAR2
    storage: Per Session (Persistent)

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

- # ====== Page Item: P0_MAT ===================================
  id: 13260219570926414
  identification: 
    name: P0_MAT
    type: Masqué

  settings: 
    valeur-protégée: true

  layout: 
    sequence: 2
    region: No Parent
    slot: BODY

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    data-type: VARCHAR2
    storage: Per Session (Persistent)

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

- # ====== Page Item: P0_HOME ==================================
  id: 16584594521419738
  identification: 
    name: P0_HOME
    type: Masqué

  settings: 
    valeur-protégée: true

  layout: 
    sequence: 13
    region: No Parent
    slot: BODY

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: SQL Query (return single value)
    sql-query: |
      SELECT 
      case when 
       T_USER.GROUPE  IN ('CONTRIBUTOR', 'ADMINISTRATOR')
       then '44'
       else '1' 
       end as k
      
       FROM T_USER where UPPER(MATRICULE) = UPPER(V('APP_USER'))
    used: Only when current value in session state is null

  session-state: 
    data-type: VARCHAR2
    storage: Per Session (Persistent)

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

- # ====== Page Item: P0_USER ==================================
  id: 70510731461264647030
  identification: 
    name: P0_USER
    type: Masqué

  settings: 
    valeur-protégée: true

  layout: 
    sequence: 1
    region: No Parent
    slot: BODY

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    data-type: VARCHAR2
    storage: Per Session (Persistent)

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

